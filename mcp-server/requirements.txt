# MCP Server Dependencies
# NOTE: This is a LIGHTWEIGHT stdio server for IDE integration.
# Heavy ML models (Qwen, Whisper, Piper) are proxied to ai-service.

# Core MCP
mcp>=0.9.0

# HTTP Client (proxy to ai-service & backend-service)
httpx>=0.26.0

# Google AI (Gemini - direct API calls, primary AI backend)
google-generativeai>=0.3.0

# Audio data handling (for STT/TTS data conversion)
numpy>=1.26.0
soundfile>=0.12.1

# Utilities
pydantic>=2.5.3
python-dotenv>=1.0.0
PyYAML>=6.0.1

# Development
pytest>=7.4.3
pytest-asyncio>=0.23.2
