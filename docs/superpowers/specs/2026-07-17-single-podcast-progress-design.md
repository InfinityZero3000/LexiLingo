# Single Podcast Progress Design

The podcast player must show playback progress once, inside the audio control panel together with the time labels and playback actions. Remove the duplicate page-level progress indicator and the decorative drag handle, because the controls are an inline card rather than a draggable sheet.

No playback state, seeking behavior, colors, or transcript behavior changes.
