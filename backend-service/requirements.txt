# FastAPI and Server
fastapi>=0.141.1
uvicorn[standard]>=0.52.4
python-multipart>=0.0.32
pypdf

# PostgreSQL (production)
asyncpg>=0.31.0
psycopg2-binary>=2.9.12

# SQLite (dev/test only)
aiosqlite>=0.22.1

# SQLAlchemy (Async ORM)
sqlalchemy[asyncio]>=2.0.52
alembic>=1.19.1

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.15.0
email-validator>=2.3.0

# Security
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
bcrypt>=5.0.0
firebase-admin>=7.5.0
google-auth>=2.56.3
google-auth-oauthlib>=1.4.0

# Environment
python-dotenv>=1.2.3

# Logging and Error Tracking
loguru>=0.7.3
sentry-sdk[fastapi,sqlalchemy,loguru]>=2.68.1

# HTTP Client (optional - for calling AI service)
httpx>=0.28.1

# RSS Feed Parsing (podcast episodes)
feedparser>=6.0.14

# News article full-text extraction
trafilatura>=2.2.0

# YouTube captions (no OAuth needed — uses public transcript endpoints)
youtube-transcript-api>=0.6.3

# Redis (async client — used by quota manager and rate limiting)
redis>=8.1.0,<9.0.0


# Background jobs — install without redis extras; redis-py is a direct dep and satisfies the transport at runtime
kombu>=5.5.0,<5.6
celery>=5.5.3,<5.6

# System metrics (used by admin monitoring endpoints)
psutil>=6.1.1

# Prometheus metrics
prometheus-fastapi-instrumentator>=7.1.0

# System metrics (used by admin monitoring endpoints)
psutil>=6.0.0

# Testing
pytest>=9.1.0
pytest-asyncio>=1.4.0
httpx>=0.27.0

# Code Quality
black>=26.5.1
isort>=8.0.1
flake8>=7.3.0
ruff>=0.16.4
