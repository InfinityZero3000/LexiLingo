# FastAPI and Server
fastapi>=0.141.1
uvicorn[standard]>=0.52.1
python-multipart>=0.0.32

# MongoDB
motor>=3.7.1,<3.8.0
pymongo>=4.17.0,<4.18.0

# Redis
redis>=7.4.1

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.14.2
defusedxml>=0.7.1
typer>=0.27.1,<1

# Google Gemini AI
google-generativeai>=0.8.6

# CORS and Security
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# Environment
python-dotenv>=1.2.2

# Logging and Error Tracking
loguru>=0.7.3
sentry-sdk[fastapi,loguru]>=2.66.1

# HTTP Client (for calling DL-Model-Support)
httpx[http2]>=0.28.1
aiohttp>=3.14.3

# Knowledge Graph (KuzuDB)
kuzu>=0.11.3

# Embeddings
sentence-transformers>=4.1.0,<5.0.0
numpy>=1.26.4,<2.0.0

# STT / TTS
faster-whisper>=1.2.1
# Native streaming primary. Kept behind an optional runtime adapter so an
# unsupported wheel degrades to Faster-Whisper instead of crashing startup.
moonshine-voice>=0.1.1,<0.2
sherpa-onnx>=1.13.5,<2
piper-tts>=1.6.0
gTTS>=2.5.4

# HuBERT / Pronunciation Analysis (transformers will auto-install torch)
# NOTE: transformers>=5.0.0 requires torch>=2.4, which is NOT available on Intel Mac (x86_64).
# Pin to <5.0.0 to stay compatible with torch 2.2.x on Intel Mac.
transformers>=4.41.0,<5.0.0
accelerate>=0.34.2
# NOTE: torch>=2.4.0 has no wheels for macOS x86_64. Max available: 2.2.2.
torch<=2.13.0,>=2.13.0
peft>=0.20.0  # LoRA fine-tuning and adapter loading

# LangGraph + LangChain (TRACECAG Orchestration)
langgraph>=1.2.10
langchain-core>=1.5.3
langchain-community>=0.4.2

# LLaMA.cpp for GGUF model inference (Vietnamese explanation)
llama-cpp-python>=0.3.34

# Audio Processing (for HuBERT resampling)
# scipy 1.18+ requires Python 3.12; CI runs Python 3.11.
scipy>=1.17.1,<1.18.0

# HTTP Downloads
requests>=2.34.2

# System Monitoring (for ResourceManager)
psutil==7.2.2

# Development
black>=26.5.1
flake8>=7.3.0
mypy>=1.20.2
pytest==9.1.1
pytest-asyncio==1.4.0
networkx>=3.6.1,<3.7.0
gliner>=0.2.28
matplotlib>=3.11.1,<4
seaborn>=0.13.2,<0.14
playwright>=1.62.0,<2
pytest-playwright>=0.8.0,<1

# Optional: Redis (if needed later)
# redis>=7.4.1
# aioredis>=2.0.1
