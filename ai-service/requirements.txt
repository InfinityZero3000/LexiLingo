# FastAPI and Server
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
python-multipart>=0.0.29

# MongoDB
motor>=3.7.1,<3.8.0
pymongo>=4.17.0,<4.18.0

# Redis
redis>=7.4.0

# Data Validation
pydantic>=2.13.4
pydantic-settings>=2.14.1

# Google Gemini AI
google-generativeai>=0.8.6

# CORS and Security
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# Environment
python-dotenv>=1.2.2

# Logging and Monitoring
loguru>=0.7.3

# HTTP Client (for calling DL-Model-Support)
httpx>=0.28.1
aiohttp>=3.13.5

# Knowledge Graph (KuzuDB)
kuzu>=0.11.3

# Embeddings
# NOTE: sentence-transformers>=4.0.0 has LRScheduler compatibility issues with torch 2.2.x.
# Pin to <4.0.0 for Intel Mac compatibility.
sentence-transformers>=5.4.1,<6.0.0
numpy>=1.23.0,<3.0.0

# STT / TTS
faster-whisper>=1.2.1
piper-tts>=1.4.2
gTTS>=2.5.4

# HuBERT / Pronunciation Analysis (transformers will auto-install torch)
# NOTE: transformers>=5.0.0 requires torch>=2.4, which is NOT available on Intel Mac (x86_64).
# Pin to <5.0.0 to stay compatible with torch 2.2.x on Intel Mac.
transformers>=5.8.0,<6.0.0
accelerate>=1.13.0
# NOTE: torch>=2.4.0 has no wheels for macOS x86_64. Max available: 2.2.2.
torch<=2.11.0,>=2.11.0
peft>=0.19.1  # LoRA fine-tuning and adapter loading

# LangGraph + LangChain (GraphCAG Orchestration)
langgraph>=1.1.10
langchain-core>=1.4.0
langchain-community>=0.4.1

# LLaMA.cpp for GGUF model inference (Vietnamese explanation)
llama-cpp-python>=0.3.22

# Audio Processing (for HuBERT resampling)
scipy>=1.17.1

# HTTP Downloads
requests>=2.33.1

# System Monitoring (for ResourceManager)
psutil==7.2.2

# Development
black>=26.3.1
flake8>=7.3.0
mypy>=1.20.2
pytest==9.0.3
pytest-asyncio==1.3.0
networkx==3.6.1
gliner>=0.2.26

# Optional: Redis (if needed later)
# redis>=7.4.0
# aioredis>=2.0.1
